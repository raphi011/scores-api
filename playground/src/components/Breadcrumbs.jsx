import React from "react";
import styled from "styled-components";
import * as Theme from "../theme";

const Breadcrumbs = () => (
    <span>
        <a href="#">Volleynet</a> / <a href="#">Tournaments</a>
    </span>
);


export default Breadcrumbs;
